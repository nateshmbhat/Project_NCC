All the forms are in the 

self.formslist

To get the fields list for the corresponding forms do this

self.nametolistsql.get( formname )
self.nametolistnotsql.get( formname )

using this in loop 

for form in self.formslist:
	sqllist = self.nametolistsql.get( form )
	notsqllist = self.nametolistnotsql.get( form )
	
	u can use these two lists in ur functions without affecting the entire program
	